// Mocked up page that I would do for something with sanity data
// Just wanted an example page to show how you would pass in the sanity data
// with the getStaticProps function

// I'd probably set up some base level template components to use for the pages as well :)

// import { urlFor } from 'lib/api';

// export default function Page({ data }) {
//   const { title, coverImage, body } = data;

//   return (
//     <article>
//       <h1>{title}</h1>
//       <img src={urlFor(coverImage)} />

//       <PortableText blocks={body} />
//     </article>
//   );
// }

// export async function getStaticProps() {
//    Get data from Sanity
// }
