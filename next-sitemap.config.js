/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://localhost:3000',
    generateRobotsTxt: true,
    // ...other options cam be added later like if the site is larger can use sitemapSize: 5000
  }